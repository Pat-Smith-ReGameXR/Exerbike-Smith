using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class QuestInfoHolder : MonoBehaviour
{
    string questDescription;

    // Start is called before the first frame update
    void Start()
    {

    }

    //this is just a short pause after the Start function that will just let other 
    //scripts run their Start() functions, this will make sure they have all their
    //necessary values 


    // Update is called once per frame
    void Update()
    {
        
    }
}
